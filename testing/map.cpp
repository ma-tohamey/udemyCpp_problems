#include <iostream>
#include <map>
#include <string>
#include <vector>
using namespace std;

int main(int argc, char *argv[]) {

  map<string, vector<string>> mp;

  int n;
  cin >> n;

  for (int i = 0; i < n; i++) {
    int name_len;
    cin >> name_len;

    for (int j = 0; j < name_len; j++) {
      string cnt_name;
      cin >> cnt_name;
    }
  }
  return 0;
}
